{
  "name": "api-gateway",
  "type": "module",
  "version": "2.0.0",
  "description": "AI API Gateway — Cloudflare Workers Edition",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.12.21"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
