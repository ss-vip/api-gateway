{
  "name": "api-gateway",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "start": "node --expose-gc app.js",
    "dev": "node --expose-gc --watch app.js",
    "restart": "node -e \"try{var f=require('fs').readFileSync('./system/temp/app.pid','utf8');process.kill(parseInt(f),'SIGTERM');console.log('[restart] killed',parseInt(f))}catch(e){console.log('[restart] no running instance')}\" && sleep 1 && node --expose-gc app.js"
  },
  "dependencies": {
    "hono": "^4.12.18",
    "@hono/node-server": "^2.0.2"
  }
}
